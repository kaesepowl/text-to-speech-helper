import React from "react";

export const ThemeProvider = function ThemeProvider({ children }) {
  return <>{children}</>;
};

export const Input = function Input({ children }) {
  return <>{children}</>;
};

export const theme = { colors: {} };
